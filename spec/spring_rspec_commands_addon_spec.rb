require "spec_helper"

describe SpringRspecCommandsAddon do
  it "has a version number" do
    expect(SpringRspecCommandsAddon::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
